# API key — contains live Anthropic credentials. NEVER commit this.
# Anchored to the repo root on purpose: a bare "config.js" also matched
# pulse/tools/phaeron-intelligence/relationship-snapshot/config.js, which holds
# no secrets and is required for that feature to load at all.
/config.js
system-globe/config.js

# Local env overrides (keep secrets out of git)
.env.local
.env.*.local

# Node
node_modules/

# Screenshots and generated assets (exclude bulk exports)
*.xlsx

# Exclude PNGs globally but allow brand assets in pulse/assets/
*.png
!pulse/assets/*.png
!pulse/tools/product-demo/assets/logos/*.png
!pulse/tools/product-demo/assets/architecture/*.png

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.url
.vercel
.env*
